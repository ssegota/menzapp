GOOGLE_CLIENT_ID=your-client-id-here.apps.googleusercontent.com
PORT=3000
# In production, set DATA_DIR to a mounted persistent volume (e.g. /data).
# Leave unset for local dev — data.json then lives next to index.js.
# DATA_DIR=/data
