VITE_GOOGLE_CLIENT_ID=your-client-id-here.apps.googleusercontent.com
# Leave empty for production same-origin deploys (server hosts the SPA).
# For local dev, point at the local Express server:
VITE_API_BASE_URL=http://localhost:3000
